import 'package:flutter/material.dart';

class ScreenForgotLoginInfo extends StatelessWidget {
  static const String path = '/recovery-acount';
  const ScreenForgotLoginInfo({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(),
    );
  }
}
